/** @deprecated 使用 formTemplateLayout.js（方案 B：导入时持久化 layout） */
export {
  buildFormTemplateLayout,
  buildMergeRenderMap,
  getLayoutCellPresentation,
  parseFormTemplateLayoutJson,
} from './formTemplateLayout.js';
